# Set a token here to protect the login flow during development
# LOGIN_TOKEN=your-secret-token

# (Optional) Cookie name for authentication token
# COOKIE_NAME=auth-token

# (Optional) Set cookie domain for cross-subdomain sharing
# COOKIE_DOMAIN=.example.com
